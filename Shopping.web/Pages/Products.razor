@page "/"
@inherits ProductBase

 @if (products == null)
{
	<h3>Loading Products</h3>
   
} 
else
{ 
    <h3 class="mb-5">Products</h3>
     var productsCount = products.Count();

    @if (productsCount > 0)
    {
        const int itemsPerRow = 4;

        var productsdto = new List<ProductDto>();

        @for(int i =0; i<productsCount; i++)
        {
            productsdto = products.Skip(i * itemsPerRow).Take(itemsPerRow).ToList();

            <div class="row mt-3">
                @foreach(var item in productsdto)
                {
                    <div class="col-md-3 mt-2">
                        <a href="#">
                            <div class="card">
                                <img class="img-thumbnail" src="@item.ImageUrl">
                                <div class="card-body">
                                    <h5 class="card-title mb-3">@item.Name</h5>
                                    <p class="card-text">
                                             <b>@item.Price.ToString("C")</b>
                                    </p>
                                </div>
                            </div>
                        </a>

                    </div>
                }
            </div>
        }

    }
    }


@* @page "/"
@inherits ProductBase

@if (products == null)
{
    <h3>Loading Products</h3>
}
else
{
    <h3 class="mb-5">Products</h3>
    <div class="row mt-3">
        @foreach (var product in products)
        {
            <div class="col-md-3 mt-2">
                <a href="#">
                    <div class="card">
                        <img class="img-thumbnail" src="@product.ImageUrl">
                        <div class="card-body">
                            <h5 class="card-title mb-3">@product.Name</h5>
                            <p class="card-text">
                                <b>@product.Price.ToString("C")</b>
                            </p>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
} *@

